# Reference repositories (cloned locally, read-only; not tracked)
docs/references/spec-kit/
docs/references/BMAD/
docs/references/OpenSpec/

# Claude commands (project-specific, not shared)
.claude/commands/

# Cursor directory
.cursor/

# Gemini directory
.gemini/
